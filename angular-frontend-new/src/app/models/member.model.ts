import {User} from './user.model';
/**
 * Represents a member in the system, extending the user.
 */
export interface Member extends User {
    /**
     * Additional fields specific to a member can be added here.
     */
}
